import '../css/app.css'

// Laravel Command Center frontend entry
document.addEventListener('DOMContentLoaded', () => {
  // noop for now - this file exists to be the Vite entry point
  console.log('Laravel Command Center assets loaded')
})
 