import '../css/app.css'

// Management panel frontend entry
document.addEventListener('DOMContentLoaded', () => {
  // noop for now - this file exists to be the Vite entry point
  console.log('Management panel assets loaded')
})
